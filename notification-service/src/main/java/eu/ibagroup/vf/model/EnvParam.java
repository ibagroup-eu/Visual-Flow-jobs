package eu.ibagroup.vf.model;

/**
 * Enum for all app variables names.
 */
public enum EnvParam {

    VF_HOST,
    EMAIL_ADDRESS,
    EMAIL_PASS,
    SMTP_HOST,
    SMTP_PORT,
    SMTP_STARTTLS,
    SMTP_AUTH,
    SLACK_TOKEN
}
